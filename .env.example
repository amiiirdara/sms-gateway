# Copy to .env for local (non-docker-compose) runs, e.g. `make run-api-gateway`.
# docker-compose.yml sets these itself for containerized services.

DATABASE_URL=postgres://sms:sms@localhost:5432/sms_gateway?sslmode=disable
REDIS_ADDR=localhost:6379
KAFKA_BROKERS=localhost:9094
CLICKHOUSE_ADDR=localhost:9000
HTTP_ADDR=:8080
