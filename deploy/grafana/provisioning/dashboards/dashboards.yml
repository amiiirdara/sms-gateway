apiVersion: 1
providers:
  - name: sms-gateway
    orgId: 1
    folder: SMS Gateway
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
